#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char ch;

	char d;

	ch = 'a';
	d = 'A';
	while
		(ch <= 'z') {
			putchar(ch);
			ch++;
		}
	while
		(d <= 'Z') {
			putchar (d);
			d++;
		}
	putchar('\n');
	return (0);
}
